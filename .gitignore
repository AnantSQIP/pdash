# ── Dependencies ──────────────────────────────────────────
node_modules/
packages/db/node_modules/

# ── Build outputs ─────────────────────────────────────────
dist/
build/
.next/
out/
*.tsbuildinfo

# ── Logs ──────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Test coverage ─────────────────────────────────────────
coverage/

# ── Prisma generated client ───────────────────────────────
**/generated/

# ── Local dev database (PGlite fallback data) ─────────────
.pglite-data/

# ── AI / agent tooling artifacts (Claude, Cursor, Windsurf, MCP) ─
.claude/
.cursor/
.windsurf/
.playwright-mcp/

# ── Session screenshots (local debug artifacts; fav.png is kept) ─
/attendance-*.png
/auth-home-*.png
/drilldown-*.png
/kpi-*.png
/org-distributions*.png
/perf-*.png

# ── Backup files (created during dev) ─────────────────────
*.bak

# ── Reference / unrelated projects (not part of this app) ─
dashboard/

# ── Binary documents ──────────────────────────────────────
*.docx
*.doc

# ── OS / editor ───────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
.cache/

# ── NOTE ──────────────────────────────────────────────────
# .env is intentionally NOT ignored here — it is committed on purpose.
# Rotate JWT_ACCESS_SECRET and the DB / MinIO passwords before any real deployment.
